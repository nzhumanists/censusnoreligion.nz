{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "coverflow-react": "^0.1.28",
    "react": "^16.2.0",
    "react-coverflow": "^0.2.3",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -l -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
